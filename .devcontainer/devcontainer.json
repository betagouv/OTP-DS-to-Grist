{
  "name": "OTP-DS-to-Grist",
  "dockerComposeFile": [
    "../docker-compose.yml",
    "./docker-compose.override.yml"
  ],
  "service": "app",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "remoteEnv": {
    "ENCRYPTION_KEY": "${localEnv:ENCRYPTION_KEY}",
    "FLASK_SECRET_KEY": "${localEnv:FLASK_SECRET_KEY}",
    "DOCKER_DATABASE_URL": "${localEnv:DOCKER_DATABASE_URL}"
  },
  "secrets": {
    "ENCRYPTION_KEY": {
      "description": "Encryption key for the application"
    },
    "FLASK_SECRET_KEY": {
      "description": "Flask secret key"
    },
    "DOCKER_DATABASE_URL": {
      "description": "Database connection URL"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "actboy168.tasks",
        "eamodio.gitlens",
        "mhutchie.git-graph",
        "aaron-bond.better-comments",
        "pkief.material-icon-theme"
      ]
    }
  },
  "forwardPorts": [5000, 5433],
  "postCreateCommand": "poetry install --with dev",
  "shutdownAction": "stopCompose"
}
