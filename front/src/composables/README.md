# composables

Composables Vue.js — fonctions réutilisables avec état réactif (`ref`, `computed`).

- Préfixe `use*`
- Un composable = une responsabilité
- Pas d'appels API directs (utiliser `utils/InternalApi`)
