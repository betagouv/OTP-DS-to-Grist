# components

Composants Vue.js de l'interface utilisateur.

Chaque composant a un fichier `.spec.js` associé dans `__tests__/`.

Les composants utilisent `<script setup>` et les conventions du Design System FR (Dsfr*).
